"use client";
import { useState } from "react";
import Link from "next/link";
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { z } from "zod";
import { ArrowRight, BarChart3, CheckCircle2, Eye, EyeOff, LockKeyhole, Mail, ShieldCheck } from "lucide-react";
import { toast } from "sonner";
import { useAuth } from "@/components/providers/auth-provider";
import { Button } from "@/components/ui/button";
import { Input, Label } from "@/components/ui/input";
const schema = z.object({ email: z.string().email("Enter a valid work email"), password: z.string().min(8, "Password must be at least 8 characters") }); type FormValues = z.infer<typeof schema>;
export default function LoginPage() { const { login } = useAuth(); const [show, setShow] = useState(false); const { register, handleSubmit, formState: { errors, isSubmitting } } = useForm<FormValues>({ resolver: zodResolver(schema) }); const submit = async (values: FormValues) => { try { await login(values.email, values.password); toast.success("Welcome back to your workspace."); } catch (error) { toast.error(error instanceof Error ? error.message : "Unable to sign in."); } };
return <div className="grid min-h-screen lg:grid-cols-[1.08fr_.92fr]"><section className="relative hidden overflow-hidden bg-[#0e3d3a] p-12 text-white lg:flex lg:flex-col lg:justify-between"><div className="absolute -right-24 -top-24 h-96 w-96 rounded-full bg-teal-300/10 blur-3xl"/><div className="absolute -bottom-20 left-20 h-80 w-80 rounded-full bg-blue-300/10 blur-3xl"/><Logo light/><div className="relative max-w-xl"><p className="mb-5 text-sm font-bold uppercase tracking-[.24em] text-teal-200">Work with clarity</p><h1 className="text-5xl font-bold leading-[1.08] tracking-tight">Every target, task and team insight in one place.</h1><p className="mt-6 text-lg leading-relaxed text-teal-50/75">Built for the pace of e-commerce operations. See what is moving, what needs attention and where your team is winning.</p><div className="mt-10 grid grid-cols-2 gap-4">{[[BarChart3,"Live performance"],[ShieldCheck,"Secure role access"],[CheckCircle2,"Daily accountability"],[ArrowRight,"Faster decisions"]].map(([Icon,label])=><div key={String(label)} className="flex items-center gap-3 rounded-xl border border-white/10 bg-white/5 p-4"><Icon className="h-5 w-5 text-teal-200"/><span className="text-sm font-semibold">{label as string}</span></div>)}</div></div><p className="relative text-xs text-teal-100/60">© {new Date().getFullYear()} 9T-Angle. Internal operations workspace.</p></section><section className="flex items-center justify-center p-6 sm:p-10"><div className="w-full max-w-md"><div className="mb-10 lg:hidden"><Logo/></div><p className="text-sm font-bold text-primary">WELCOME BACK</p><h2 className="mt-2 text-3xl font-bold tracking-tight">Sign in to 9T-Angle</h2><p className="mt-2 text-sm text-muted-foreground">Use your company account to continue.</p><form onSubmit={handleSubmit(submit)} className="mt-8 space-y-5"><div><Label htmlFor="email">Work email</Label><div className="relative"><Mail className="absolute left-3 top-3 h-4 w-4 text-muted-foreground"/><Input id="email" type="email" autoComplete="email" placeholder="you@9tangle.com" className="pl-10" {...register("email")}/></div>{errors.email && <p className="mt-1.5 text-xs text-destructive">{errors.email.message}</p>}</div><div><div className="flex justify-between"><Label htmlFor="password">Password</Label><Link href="/forgot-password" className="text-xs font-semibold text-primary hover:underline">Forgot password?</Link></div><div className="relative"><LockKeyhole className="absolute left-3 top-3 h-4 w-4 text-muted-foreground"/><Input id="password" type={show?"text":"password"} autoComplete="current-password" placeholder="Enter your password" className="px-10" {...register("password")}/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-2.5 p-0.5 text-muted-foreground" aria-label={show?"Hide password":"Show password"}>{show?<EyeOff className="h-4 w-4"/>:<Eye className="h-4 w-4"/>}</button></div>{errors.password && <p className="mt-1.5 text-xs text-destructive">{errors.password.message}</p>}</div><Button size="lg" className="w-full" disabled={isSubmitting}>{isSubmitting?"Signing in…":<>Sign in securely <ArrowRight className="h-4 w-4"/></>}</Button></form><p className="mt-8 text-center text-xs leading-relaxed text-muted-foreground">Protected by encrypted authentication and role-based access.<br/>Contact your administrator if you need an account.</p></div></section></div> }
function Logo({light=false}:{light?:boolean}) { return <div className="relative flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-xl bg-primary font-black text-white shadow-lg shadow-black/10">9T</div><div><p className={`text-lg font-bold leading-none ${light?"text-white":"text-slate-950"}`}>9T-Angle</p><p className={`mt-1 text-[10px] font-semibold uppercase tracking-[.18em] ${light?"text-teal-100/60":"text-muted-foreground"}`}>Operations workspace</p></div></div> }
