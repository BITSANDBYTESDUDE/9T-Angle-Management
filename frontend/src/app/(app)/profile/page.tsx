"use client";
import { useEffect, useState } from "react";
import { KeyRound, Save, UserCircle } from "lucide-react";
import { toast } from "sonner";
import { patch } from "@/lib/api";
import { useAuth } from "@/components/providers/auth-provider";
import { PageHeader } from "@/components/shared/page-header";
import { Avatar } from "@/components/ui/avatar";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input, Label } from "@/components/ui/input";
export default function ProfilePage(){const {user,refresh}=useAuth();const employee=user?.employee;const [profile,setProfile]=useState({fullName:"",phone:"",profileImage:"",address:"",timezone:"UTC"});const [password,setPassword]=useState({currentPassword:"",newPassword:""});const [loading,setLoading]=useState(false);useEffect(()=>{if(employee)setProfile({fullName:employee.fullName||"",phone:employee.phone||"",profileImage:employee.profileImage||"",address:employee.address||"",timezone:employee.timezone||"UTC"})},[employee]);async function saveProfile(e:React.FormEvent){e.preventDefault();setLoading(true);try{const r=await patch("/auth/profile",profile);toast.success(r.message);await refresh()}catch(e){toast.error(e instanceof Error?e.message:"Unable to save profile.")}finally{setLoading(false)}}async function savePassword(e:React.FormEvent){e.preventDefault();if(password.newPassword.length<8)return toast.error("New password must be at least 8 characters.");setLoading(true);try{const r=await patch("/auth/change-password",password);toast.success(r.message);setPassword({currentPassword:"",newPassword:""})}catch(e){toast.error(e instanceof Error?e.message:"Unable to change password.")}finally{setLoading(false)}}return <div className="space-y-6"><PageHeader eyebrow="Your account" title="Profile & security" description="Keep your contact details and password up to date."/><div className="grid gap-5 xl:grid-cols-[.7fr_1.3fr]"><Card><CardContent className="flex flex-col items-center p-7 text-center"><Avatar name={employee?.fullName||"Administrator"} src={employee?.profileImage} className="h-24 w-24 text-xl"/><h2 className="mt-4 text-xl font-bold">{employee?.fullName||"9T-Angle Administrator"}</h2><p className="mt-1 text-sm text-muted-foreground">{user?.email}</p><Badge value={user?.role} className="mt-3"/>{employee&&<div className="mt-6 w-full rounded-xl bg-slate-50 p-4 text-left text-sm"><p className="text-xs text-muted-foreground">Position</p><b>{employee.position}</b><p className="mt-3 text-xs text-muted-foreground">Department</p><b>{employee.department?.name}</b></div>}</CardContent></Card><div className="space-y-5">{employee&&<Card><CardHeader><CardTitle className="flex items-center gap-2"><UserCircle className="h-5 w-5 text-primary"/> Personal details</CardTitle></CardHeader><CardContent><form onSubmit={saveProfile} className="grid gap-4 sm:grid-cols-2"><Field label="Full name"><Input value={profile.fullName} onChange={e=>setProfile({...profile,fullName:e.target.value})} required/></Field><Field label="Phone"><Input value={profile.phone} onChange={e=>setProfile({...profile,phone:e.target.value})}/></Field><Field label="Profile image URL"><Input value={profile.profileImage} onChange={e=>setProfile({...profile,profileImage:e.target.value})}/></Field><Field label="Timezone"><Input value={profile.timezone} onChange={e=>setProfile({...profile,timezone:e.target.value})}/></Field><div className="sm:col-span-2"><Field label="Address"><Input value={profile.address} onChange={e=>setProfile({...profile,address:e.target.value})}/></Field></div><div className="sm:col-span-2"><Button disabled={loading}><Save className="h-4 w-4"/> Save profile</Button></div></form></CardContent></Card>}<Card><CardHeader><CardTitle className="flex items-center gap-2"><KeyRound className="h-5 w-5 text-primary"/> Change password</CardTitle></CardHeader><CardContent><form onSubmit={savePassword} className="grid gap-4 sm:grid-cols-2"><Field label="Current password"><Input type="password" value={password.currentPassword} onChange={e=>setPassword({...password,currentPassword:e.target.value})} required/></Field><Field label="New password"><Input type="password" value={password.newPassword} onChange={e=>setPassword({...password,newPassword:e.target.value})} required/></Field><div className="sm:col-span-2"><Button variant="outline" disabled={loading}>Update password</Button></div></form></CardContent></Card></div></div></div>}
function Field({label,children}:{label:string;children:React.ReactNode}){return <div><Label>{label}</Label>{children}</div>}
