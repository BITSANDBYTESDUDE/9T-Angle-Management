import type { LucideIcon } from "lucide-react";
import { ArrowDownRight, ArrowUpRight } from "lucide-react";
import { Card } from "../ui/card";
import { cn } from "@/lib/utils";
export function StatCard({ label, value, helper, icon: Icon, tone = "teal", change }: { label: string; value: string | number; helper?: string; icon: LucideIcon; tone?: "teal" | "blue" | "amber" | "rose" | "purple"; change?: number }) { const tones = { teal: "bg-teal-50 text-teal-700", blue: "bg-blue-50 text-blue-700", amber: "bg-amber-50 text-amber-700", rose: "bg-rose-50 text-rose-700", purple: "bg-violet-50 text-violet-700" }; return <Card className="relative overflow-hidden p-5"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-wider text-muted-foreground">{label}</p><p className="mt-2 text-3xl font-bold tracking-tight">{value}</p></div><div className={cn("rounded-xl p-2.5", tones[tone])}><Icon className="h-5 w-5" /></div></div>{helper && <p className="mt-3 text-xs text-muted-foreground">{helper}</p>}{change !== undefined && <span className={cn("mt-3 inline-flex items-center text-xs font-bold", change >= 0 ? "text-emerald-600" : "text-rose-600")}>{change >= 0 ? <ArrowUpRight className="h-3.5 w-3.5"/> : <ArrowDownRight className="h-3.5 w-3.5"/>}{Math.abs(change)}%</span>}</Card>; }
